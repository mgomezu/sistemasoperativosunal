import os

os.getenvb(key, default=None)

#funcion: Devuelve el valor de la llave key de la variable de entorno si existe, o el valor predeterminado si no existe, 
#la llave, el default y el valor son en bytes.